{
  "name": "copy-tweets",
  "version": "1.0.0",
  "description": "Browser extension for copying tweets",
  "type": "module",
  "scripts": {
    "dev": "bun build --entrypoints ./src/background.ts --entrypoints ./src/content.ts --entrypoints ./src/sidebar.ts --outdir ./out --watch"
  },
  "dependencies": {
    "@types/chrome": "^0.0.260",
    "@types/webextension-polyfill": "^0.10.0",
    "webextension-polyfill": "^0.10.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
